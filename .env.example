# Contact-form delivery through Resend. Keep real values in .env.local or the deployment environment.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# Optional external scheduling page. Without this, discovery-call CTAs move visitors to the enquiry form.
NEXT_PUBLIC_DISCOVERY_CALL_URL=
